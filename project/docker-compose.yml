version: '3.8'

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80" # Maps port 80 inside the container (Nginx) to port 8080 on your host machine
    container_name: portfolio-app
    restart: unless-stopped
